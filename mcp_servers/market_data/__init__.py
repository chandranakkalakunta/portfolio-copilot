"""Market-data MCP server package (yfinance provider, ADR-0005).

Monorepo path: ``mcp_servers/market_data/``. Import as ``market_data`` (see
pythonpath). Parent dir is ``mcp_servers/`` so ``import mcp`` is the PyPI SDK.
"""

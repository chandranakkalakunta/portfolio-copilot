# Local Claude session staging (do not commit)
claude-working/

# Local secrets / run config (commit .env.example instead)
.env
.env.*
!.env.example

# Python / tooling
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
.terraform.tfstate.lock.info

# Coverage
.coverage
.coverage.*
htmlcov/
coverage.xml

"""Market-data MCP server package (yfinance provider, ADR-0005).

Monorepo path: ``mcp/market_data/``. Import as ``market_data`` (see pythonpath)
to avoid shadowing the official PyPI ``mcp`` SDK package name.
"""
